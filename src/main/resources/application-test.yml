# Test Profile Configuration
# This profile enables demo data for testing

spring:
  profiles:
    active: test

iqscaffold:
  liquibase:
    # Include demo context for test data
    contexts: "demo"

logging:
  level:
    com.iqscaffold.contactservice.tenancy.TenantLiquibaseRunner: DEBUG
    liquibase: DEBUG